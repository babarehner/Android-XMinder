<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="cardioTextBG">
        <item name="android:textColor">@color/textColorCardio</item>
        <item name="android:background">@color/bgColorCardio</item>
    </style>

    <style name="strengthTextBG">
        <item name="android:textColor">@color/textColorStrength</item>
        <item name="android:background">@color/bgColorStrength</item>
    </style>

</resources>
